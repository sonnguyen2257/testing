pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Starting build...'
            }
        }

        stage('Establish SSH Connection'){
            steps {
                echo 'Establishing SSH connection...'
            }
        }
    }
}